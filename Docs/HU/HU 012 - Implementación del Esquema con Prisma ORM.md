Como Full Stack Developer,
Quiero traducir el modelo DER al lenguaje de Prisma y ejecutar las migraciones,
Para generar el cliente de TypeScript y sincronizar la base de datos física con el código.

Criterios de Aceptación:

Inicializar Prisma en el workspace de packages/database.

Definar los modelos (Models) y Enums en el archivo schema.prisma.

Ejecutar la migración inicial (npx prisma migrate dev).

Generar el cliente de Prisma para que sea consumido por el API de NestJS.